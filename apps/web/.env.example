# Next loads env from the monorepo root first (see next.config.ts), then apps/web.
# You can copy the repo-root `.env.example` to `../../.env.local` or place vars here.

INTENTVAULT_SIGNALS_MODE=auto
INTENTVAULT_INFERENCE_MODE=auto
SOLROUTER_API_KEY=
SOLROUTER_MODEL=gpt-oss-20b
SOLROUTER_BASE_URL=
